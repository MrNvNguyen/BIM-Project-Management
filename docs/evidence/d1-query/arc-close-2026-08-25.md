# D1 query optimization — kết quả arc (2026-08-25)

## Đã ship (engineering)

| Wave | Nội dung | Trạng thái |
|------|----------|------------|
| 0 | EXPLAIN baseline hot paths | PASS (Insights prod NOT_MEASURED) |
| 1a | Labor helpers O(1) trong `src/finance.ts` | SHIPPED |
| 1b | Finance GET labor + `fiscalMonthsSQLFilter` half-open | SHIPPED |
| 2 | JOIN pre-agg `/api/projects`, `/api/executive/projects`; shared-costs IN; weekly-plan batch; legal không `ld.*` | SHIPPED |
| 3 | `migrations/0047_timesheets_work_date_indexes.sql` | SHIPPED — local EXPLAIN: **SEARCH** `idx_timesheets_work_date` |
| 4 | GET legal overview **không** migrate schema | SHIPPED (batch leave/resync deferred) |

## Product evidence

| Metric | Status |
|--------|--------|
| Layer A vitest labor falsifiers | **20/20 PASS** |
| Layer B đồng parity (4 finance endpoints) | **NOT_MEASURED** — cần seed + admin token |
| D1 Insights prod `avgRowsRead` | **NOT_MEASURED** — cần `CLOUDFLARE_API_TOKEN` |

## Còn lại (ngoài box / follow-up)

- `POST .../labor-costs/sync` vẫn `strftime` (write path)
- Một số dashboard/analytics `strftime`
- Wave 4 còn: `db.batch` leave approve, resync feePct preload

## Apply migration prod

```text
wrangler d1 migrations apply bim-management-production
# rồi PRAGMA optimize một lần (maintenance)
```
