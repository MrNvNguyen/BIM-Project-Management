# Strategy advisor — D1 query Wave 1a adjudicate + authorize 1b

**Date:** 2026-08-25  
**Authority:** D1 plan SSOT `tối_ưu_d1_query_ccc324f3.plan.md` + prior `GO-WITH-GATES` (no signed `0 Documents/1-Sprints/`)  
**PO-human:** **NO** — not Success/Ceiling; do not send `STRATEGY_PO_PACKET`

```yaml
strategyOutputType: STRATEGY_APPROVE_EXECUTION
wave_1a_verdict: ENGINEERING_ACCEPT / PRODUCT_NOT_CLOSED
challenged_assumption: >-
  "Wave 1a PASS + vitest 20/20 means finance/dashboard loads are now O(1)
  timesheet queries and đồng-safe." Code shows the three helpers are O(1), but
  the user-visible finance GETs still month-loop + strftime; tests never hit D1;
  Layer B parity table is still all NOT_MEASURED.
po_packet_precheck: N/A
next_agent: technical-advisor
```

---

## Independent claims (from code/evidence, not team narrative)

| id | statement | evidence | status |
|---|---|---|---|
| C1 | The three labor helpers in `src/finance.ts` use 1 MLC + 1–2 date-range `GROUP BY` queries (`work_date >= ? AND work_date < ?`), not `for (month)` × 2 SQL. | `fetchCompanyEffHoursByMonth`, `fetchProjectHoursByMonth`, `fetchAllProjectsHoursByMonth`; `computeRealtimeLaborByProject` / `computeProjectLaborFromTimesheets` | **measured** (code) |
| C2 | Labor “parity” tests are pure functions only — they do not execute the new SQL. | `src/finance.test.ts` 7 labor cases call `computeProjectLaborFromAggregates` / `computeRealtimeLaborFromAggregates` / `filterMlcMonths` / `dayAfter`. Wave 0 parity table: all `NOT_MEASURED`. | **measured** (tests + evidence doc) |
| C3 | Hottest remaining finance GETs still do M×2 `strftime` SCAN loops. | `labor-costs-all-projects` ~4947–4980; `costs-revenue-summary` ~5154–5177; `/api/finance/project/:id` ~9268–9275; `fiscalMonthsSQLFilter` ~5897–5904 | **measured** (code) |
| C4 | Company-wide range still **SCAN** — `idx_timesheets_work_date` was dropped in 0022 and never recreated. No `migrations/0047*.sql`. | Wave 0 Q4 EXPLAIN; `migrations/0022_fix_timesheets_unique_constraint.sql`; glob 0047 = 0 files | **measured** |
| C5 | Wave 1a is **uncommitted** — pre-1a SQL is recoverable from `HEAD`. | `git status`: `M src/finance.ts src/finance.test.ts src/index.tsx` | **measured** |
| C6 | `GET /api/legal/:projectId/overview` still **writes** via `migrateOldProjectToPackages`. | `src/index.tsx` ~12281 | **measured** |
| C7 | D1 Insights prod `avgRowsRead` = **NOT_MEASURED**. | `docs/evidence/d1-query/wave0-baseline.md` | **NOT_MEASURED** |

**Invalid environment:** Insights (no `CLOUDFLARE_API_TOKEN`) — does not spend attempt cap.  
**Valid engineering pass:** helper rewrite + round-per-month / OT / leave-in-denominator / `dayAfter` unit cases.  
**Valid product pass:** none (no Layer B đồng, no `rows_read` on a seeded finance load).  
**NOT_MEASURED:** endpoint Δ đồng; Insights; post-1a EXPLAIN beyond a 3-line paste inside the Wave 0 file.

---

## Wave 1a verdict

**Do not revert.** Helpers belong in `finance.ts`; `index.tsx` imports `computeRealtimeLaborByProject` and wraps `computeMonthLaborCost`. Allocation math in the pure helpers matches TU-DIEN (round per calendar month, then sum; KPI from timesheets × `monthly_labor_costs`, not `project_labor_costs`).

**Do not call Wave 1a product PASS or “optimization complete.”**

- Prior GO-WITH-GATES required a đồng snapshot **before** 1a code. That snapshot was never filled; the Wave 0 evidence file was then overwritten with `Wave 1a verdict: SHIPPED`. That mixing is rejected as a close record.
- `npm test` 20/20 (20 `it(` in `finance.test.ts`) is Layer A only.
- Callers that **did** move: `financial-by-project`, dashboard top-projects labor, lifetime labor, `estimate-vs-actual` via `computeProjectLaborFromTimesheets`. Those screens still have **no** recorded JSON Δ.
- NTC `start_day ≠ 1`: new helpers clip timesheets to `dateFrom`/`dayAfter(dateTo)`; old loops used **full calendar months** via `strftime`. Default is day=1 (equivalent). Tests do not cover mid-month NTC start. Retained risk — do not “fix” by switching 1b loops to `fyStart` clipping.

**Attempt accounting:** Wave 0 observe-only (no spend). Wave 1a = 1 engineering attempt in class `labor-strftime-month-loop`. Product attempt for 1a = `NOT_MEASURED` (does not spend). Cap 3 measured in class; 1b is the same class.

---

## Challenged assumption

Team assumption: consolidating the three helpers **closes** the P0 finance-load cost and is formula-safe because tests pass.

**Counter:** `GET /api/projects/:id/costs-revenue-summary` and `GET /api/financial-summary/labor-costs-all-projects` are the finance screens that still run **12 × (MLC + proj SCAN + company SCAN)** with `strftime` in WHERE. Wave 1a did not touch them. Claiming O(1) “financial load” from helper tests is a product-mapping fail.

Second challenge: plan 1b laundry-list (including `fix-inconsistency` **writes** and `team-productivity`) is **more than one lever**. Strategy splits writes to Wave 4 and defers analytics.

---

## ARG

```yaml
ARG:
  product_mapping: "User sees same đồng on finance/dashboard; fewer D1 rows / faster load — not yet proven on a screen"
  physical_metric_definition: "Labor KPI = computeProjectLaborFromTimesheets path (TU-DIEN); D1 cost = rows scanned, not statement count"
  measured_owner: co-dominant
    # remaining: month-loop strftime on finance GETs (1b) + correlated COUNT on project lists (2) + missing work_date index (3)
  lever_upper_bound: >-
    1b can remove M×2 SCAN on remaining finance GETs (query-count + strftime-in-WHERE).
    Cannot make company-wide range SEARCH without an index (Wave 3). Cannot close list N×subquery (Wave 2).
  retained_contract_risk:
    - NTC start_day != 1 clipping vs full-month strftime
    - substr(work_date,1,4)/(6,2) assumes ISO YYYY-MM-DD
    - GET labor-costs still prefers project_labor_costs cache (TU-DIEN forbids cache for KPI) — pre-existing; do not silently switch
    - consistency-check uses RAW hours × cph (not eff hours) — do not silent-fix
  aps_hard_limits_respected: n/a  # BIM PM, not Forma
  measured_attempts_spent: 1  # engineering 1a; product 1a NOT_MEASURED
  grind_verdict: continue  # discriminator exists: remaining GET loops vs helpers
```

---

## TEAM_DIRECTIVE (sequenced; Wave 1b is the only open lever)

### Gate 0 — blocking, no 1b SQL until done

Wave 1a is **uncommitted**. Pre-1a is `git show HEAD:src/finance.ts` / `HEAD:src/index.tsx`.

QA (or lead under QA protocol) must write **`docs/evidence/d1-query/wave1a-parity.md`** with:

1. Fingerprint: git SHA of HEAD + note working-tree 1a; local D1; seed used.
2. Authenticated JSON (redact tokens) for **one fixture project**, twice: **HEAD (pre-1a)** vs **working tree (post-1a)** for:
   - `GET /api/projects/:id/estimate-vs-actual` (labor actual)
   - `GET /api/projects/:id/costs-revenue-summary` (expect **Δ possible / still old loop** — record anyway)
   - `GET /api/analytics/financial-by-project?year=<FY>` (`labor_cost`)
   - `GET /api/finance/project/:id` (labor fields)
3. Verdict per field: `Δ = 0` | `Δ ≠ 0` | `NOT_MEASURED` (auth/seed fail).

**Stop:** If 1a-touched endpoints show `Δ ≠ 0` đồng → **do not start 1b**. Report-up strategy (`REJECT_AND_DIRECT` repair formula).  
**Stop:** If freeze is skipped and 1b edits `index.tsx` anyway → process FAIL; isolation of 1a is destroyed.

If seed/auth cannot run: record `NOT_MEASURED` + reason; strategy already accepts 1a engineering — **1b may proceed** only with that explicit gap labeled. Do not invent Δ=0.

Do **not** append 1a close-out into `wave0-baseline.md`.

### Wave 1b — AUTHORIZED (one lever)

```yaml
WAVE_CARD_INTENT:
  wave_id: d1-query-w1b
  measured_owner: finance-GET-labor-month-loop
  one_allowed_lever: >-
    Remaining finance GET labor allocation: stop per-month strftime WHERE;
    reuse finance.ts fetch*/compute* aggregators (range + GROUP BY; round per month then sum).
    Convert fiscalMonthsSQLFilter to OR of half-open monthDateRange predicates (no strftime on date cols).
  in_scope:
    - GET /api/projects/:id/costs-revenue-summary labor loop
    - GET /api/financial-summary/labor-costs-all-projects month loop
    - GET /api/finance/project/:id labor loops (including mode=months)
    - fiscalMonthsSQLFilter (costs/revenues filters on those GETs)
    - GET labor-costs / labor-costs-yearly: range-rewrite uncached SQL only — do not change cache-first behavior
  out_of_scope:
    - NOT all ~113 strftime
    - team-productivity (analytics; defer)
    - POST fix-inconsistency / labor-costs/sync (Wave 4 writes)
    - GET /api/projects and /api/executive/projects JOIN (Wave 2)
    - migration 0047 (Wave 3)
    - GET legal migrate (Wave 4)
    - VAT / syncPaymentToRevenue / computeBookedRevenue
    - silent-fix consistency-check raw×cph
    - switching KPI onto or off project_labor_costs cache
  prediction: >-
    After 1b, named finance GETs issue O(1) timesheet aggregates per request
    (not M×2). EXPLAIN of those aggregates: no strftime(work_date) in WHERE.
    Layer B Δ=0 vs Gate 0 post-1a snapshot on labor fields.
  falsifier: >-
    Still a for-month prepare() on timesheets with strftime in WHERE on an in-scope GET;
    or labor đồng Δ≠0 vs Gate 0 snapshot.
  contracts_at_risk: [finance-ssot, d1-query]
  qa_tier: Q2
  q2_class: PRODUCT_SPOT
  attempt_number: 2/3  # same class as 1a
  stop_rule: >-
    One measured 1b attempt. Δ≠0 → revert 1b only, keep 1a helpers, report-up.
    Do not start Wave 2 in the same diff. Do not add indexes.
  preserve_semantics: >-
    Selected months = FULL calendar months via monthDateRange(calYear,calMonth),
    NOT fyStart/fyEnd clipping (unless start_day=1 and all_months — then equivalent).
```

Technical writes the formal `WAVE_CARD` then lead implements **only after Gate 0 file exists** (even if some rows are `NOT_MEASURED`).

### Wave 2 — authorized **after** 1b Q2 PASS (not now)

JOIN pre-agg `GET /api/projects` + `GET /api/executive/projects`; narrow legal `SELECT ld.*`; weekly-plan assignee `IN (...)`; `GET /api/shared-costs` allocations one query.

**Stop / contracts:** `computed_progress` stays task done/open (TU-DIEN). Do not drop FE columns without checking `app.js`. Do not treat `collected_amount` / `booked_revenue` / `acceptance_amount` as interchangeable. Do not add 0047.

### Wave 3 — **BLOCKED**

**What blocks 0047:**

1. Plan + skill: indexes only if **EXPLAIN after Wave 1–2 rewrite** still SCAN on the **new** SQL.
2. No 0047 file must appear before that EXPLAIN pack (`docs/evidence/d1-query/wave3-explain.md`).
3. Empty-table EXPLAIN is enough to prove missing `work_date` index (Wave 0 Q4 already did for old SQL). After 1b, **re-EXPLAIN the new aggregates** (company-wide range + project+range + JOIN preagg). Add only indexes that still SCAN.
4. Insights remains `NOT_MEASURED` — must not invent `avgRowsRead`; must not block 1b; **must not** justify a kitchen-sink 0047.
5. `PRAGMA optimize` once after index apply — not per-request.
6. Do not recreate every FK index. Candidate list stays: `(work_date)` and/or `(project_id, work_date)` if proven; tasks composite only if Wave 2 JOIN still SCAN; do not duplicate `idx_payment_requests_status`.

### Wave 4 — **BLOCKED** until 1b money GETs are closed (prefer after 2)

**What blocks Wave 4:**

1. Mixing write-batch / GET-side-effect removal with labor formula rewrite → cannot attribute đồng Δ. 1b must be closed first.
2. `fix-inconsistency` labor UPSERT is a **write** lever (plan Wave 4), even though the 1b bullet list named it — **strategy overrides the list**.
3. `GET /api/legal/:projectId/overview` ~12281 `migrateOldProjectToPackages` — FORBID on GET; move to existing POST migrate. QA: GET overview `rows_written = 0` (no DELETE/INSERT schema).
4. Leave-day UPSERT `db.batch` / `ON CONFLICT` — 0044 unique already warned duplicates; run on **local/copy D1**, not prod first.
5. `batch()` does not reduce rows written of the same INSERTs — latency/subrequest only. Do not claim billing write-win.

---

## PO-human

**Escalate = NO.**

`PO_PACKET_PRECHECK` if someone tried Success now:

```yaml
PO_PACKET_PRECHECK:
  decisive_metrics_measured: false   # Layer B đồng, rows_read, Insights
  product_evidence_supports_claim: false  # remaining finance GETs still M×2 SCAN
  run_class_valid_for_product_claim: false
  open_NOT_MEASURED_on_decision_metric: true
  technical_options_to_PO: false
  result: FAIL → no packet
```

**Framing for PO later (when Success packet is actually allowed):**

- **Kết quả (product):** Số tiền trên màn hình tài chính / dashboard **không đổi** (cùng công thức từ điển: lương = timesheet × quỹ tháng; tách nghiệm thu / vào sổ / dòng tiền). Màn hình tài chính và danh sách dự án **tải nhanh hơn** vì D1 quét ít hàng hơn (không còn vòng tháng `strftime`, không còn COUNT lặp theo từng dự án).
- **Không nói với PO:** O(1), SCAN vs SEARCH, 0047, vitest 20/20, Insights token, Option A/B/C.

---

## next_agent

`technical-advisor` — emit `WAVE_CARD` for `d1-query-w1b` matching this directive; remind lead **Gate 0 before code**. Then `qa-agent` Gate 0 (or lead+qa). After 1b code: `qa-agent` Q2 → technical report-up → strategy only if Δ≠0 / scope creep / Wave 2 ask.

```yaml
STRATEGY_FOOTER:
  strategyOutputType: STRATEGY_APPROVE_EXECUTION
  challenged_assumption: Wave 1a PASS implies finance loads are O(1) and đồng-closed
  po_packet_precheck: N/A
  next_agent: technical-advisor
```
