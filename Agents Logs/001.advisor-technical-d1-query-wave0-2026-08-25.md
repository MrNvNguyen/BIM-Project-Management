# Technical advisor — D1 query Wave 0

**Date:** 2026-08-25  
**Directive:** Strategy `GO-WITH-GATES` — Wave 0 OBSERVE_ONLY  
**Evidence:** `docs/evidence/d1-query/wave0-baseline.md`

## PO-visible problem

Load tài chính / labor và danh sách dự án quét `timesheets`/`tasks` lặp theo tháng hoặc theo từng dự án → rows read D1 cao, latency dashboard.

## Owner profile

```yaml
owner_profile:
  DOMINANT_OWNER: labor-month-loop + strftime-scan (computeRealtimeLaborByProject)
  TWO_CO_DOMINANT_OWNERS:
    - computeProjectLaborFromTimesheets (N+1 tháng)
    - correlated subquery GET /api/projects + /api/executive/projects
```

## WAVE_CARD (Wave 0 — closed)

```yaml
wave_id: d1-query-w0
prediction: EXPLAIN local sẽ cho SCAN trên strftime(work_date) và correlated subquery; Insights prod NOT_MEASURED nếu thiếu token
falsifier: strftime path SEARCH idx_timesheets_work_date — falsified (index không tồn tại sau 0022)
lever: none (observe only)
stop_rule: baseline doc + 4 EXPLAIN + index inventory
qa_tier: measurement / no product code
regression: n/a
```

## Attempt outcome

| Check | Result |
|---|---|
| Hot SQL inventory | PASS |
| EXPLAIN × 4 patterns | PASS (local, empty tables) |
| D1 Insights | NOT_MEASURED |
| Parity đồng | NOT_MEASURED |
| Code change | none (correct) |

**Verdict:** `PASS_PARTIAL`

## open_gaps

1. `CLOUDFLARE_API_TOKEN` for `wrangler d1 insights`
2. Parity baseline numbers — cần seed local hoặc staging fixture + admin token
3. `meta.rows_read` optional trace — defer unless strategy directs

## Route

**REPORT-UP-STRATEGY** — Wave 0 đủ để authorize Wave 1a labor consolidation; Insights/parity có thể song song QA.

## Next commands (lead-dev Wave 1a)

```bash
cd c:\Users\NguyenNguyenVan\Documents\GitHub\BIM-Project-Management

# 1. Parity baseline (trước code)
npm run db:migrate:local
npm run db:seed
npm run dev:sandbox
# Ghi JSON 4 endpoint labor cho project_id=1 → paste vào wave0-baseline parity table

# 2. Implement (Wave 1a only)
# - Gộp computeRealtimeLaborByProject + computeProjectLaborFromTimesheets → 1–2 GROUP BY date range
# - Không thêm migration 0047

# 3. Verify
npm test
npx wrangler d1 execute bim-management-production --local --command "EXPLAIN QUERY PLAN <new aggregate SQL>"
# Parity: Δ=0 đồng vs baseline
```
