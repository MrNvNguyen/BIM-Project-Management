# Strategy advisor — D1 query arc close (Ceiling)

**Date:** 2026-08-25  
**Authority:** Plan/GO-WITH-GATES (no signed `0 Documents/1-Sprints/`); prior Wave 1a `STRATEGY_APPROVE_EXECUTION`  
**PO-human:** **NO send** — Telegram forbidden this turn. Packet text below is **draft only** for when PO asks.  
**Model:** intended L1 strategy-advisor · actual inherit (this run) · fallback n/a

```yaml
strategyOutputType: STRATEGY_CLOSE_CEILING
product_ceiling_claimed: false
challenged_assumption: >-
  QA READY_FOR_STRATEGY_SUCCESS_PARTIAL + waves 1a–4 SHIPPED means the D1
  product outcome is close enough for Success or Success-Partial.
po_packet_precheck: FAIL
next_agent: none
```

---

## 1. PO contract / box

- **Product goal (from Wave 0 technical):** màn hình tài chính / labor và danh sách dự án quét ít hàng D1 hơn, **số đồng không đổi** (TU-DIEN: lương = timesheet × quỹ tháng; tách nghiệm thu / vào sổ / dòng tiền).
- **Signed campaign folder:** absent → research/plan only; behavior already shipped in working tree under prior GO-WITH-GATES.
- **Bars (original):** Wave 0 EXPLAIN; Gate 0 Layer B Δ=0 đồng; Insights prod; indexes only after post-rewrite SCAN; one lever per wave; max 3 measured attempts / class.
- **Out of this box (already named):** `POST .../labor-costs/sync` strftime; remaining dashboard/analytics strftime; D1 Insights prod.

---

## 2. Claims from raw evidence (not team narrative)

| id | statement | evidence | status |
|---|---|---|---|
| C1 | Wave 0 EXPLAIN: `strftime` on `work_date` → **SCAN timesheets**; company-wide range SCAN because `idx_timesheets_work_date` was dropped in 0022. | `docs/evidence/d1-query/wave0-baseline.md` Q1, Q4 | **measured** |
| C2 | Gate 0 labor đồng on 4 finance endpoints is **empty**. | `docs/evidence/d1-query/wave1a-parity.md` — all cells `NOT_MEASURED` | **NOT_MEASURED** |
| C3 | D1 Insights prod (`avgRowsRead`) never ran (`CLOUDFLARE_API_TOKEN` missing). | Wave 0 Insights section | **NOT_MEASURED** |
| C4 | Layer A labor falsifiers: vitest 20/20; tests call pure helpers, not D1/HTTP. | QA log; `src/finance.test.ts` `labor allocation` cases | **measured** (engineering only) |
| C5 | In-scope list routes rewritten to JOIN pre-agg; three money fields still separate on executive list. | `GET /api/projects` ~1327 JOIN `ts`/`mc`; `GET /api/executive/projects` ~15590 JOIN `pay`/`rev` (`collected_amount` / `booked_revenue` / `acceptance_amount`) | **measured** (code) |
| C6 | Legal overview GET does not call migrate; migrate is POST-only. | `src/index.tsx` ~12118 comment; `migrateOldProjectToPackages` only at POST ~12112 | **measured** (code) |
| C7 | Post-0047 local EXPLAIN: **SEARCH** `idx_timesheets_work_date` on half-open range. | Mission raw + `docs/evidence/d1-query/arc-close-2026-08-25.md` (no `wave3-explain.md` pack as previously required) | **measured** (planner) · pack **missing** |
| C8 | `GET /api/dashboard/cost-summary` still runs **12×** `strftime` SCAN loops (duplicate of pre-1a P0). | `src/index.tsx` ~7964 / ~8035–8051 | **measured** (code) |
| C9 | `POST /api/projects/:id/labor-costs/sync` still `strftime` per month. | `src/index.tsx` ~4738–4764, ~4820–4824 | **measured** (code) |
| C10 | `GET /api/analytics/financial-by-project` still correlated task COUNT (not Wave 2 routes). | `src/index.tsx` ~10932–10933 | **measured** (code) |

**Invalid environment:** Insights token; Layer B seed/admin Bearer / running Worker+D1. Does **not** spend product attempt cap.

**Valid product failure:** none recorded (no Layer B run).

**Valid product pass:** **none**. No screen JSON Δ đồng; no `rows_read` / Insights before-after.

**NOT_MEASURED (decision metrics for Success):** Layer B đồng; prod Insights; user-visible latency.

---

## 3. Attempt accounting

| Wave | Class | Outcome | Spend? |
|---|---|---|---|
| 0 | observe EXPLAIN | PASS (Insights NOT_MEASURED) | no |
| 1a | `labor-strftime-month-loop` | engineering PASS; product NOT_MEASURED | **1** engineering |
| 1b | same class (finance GETs) | engineering PASS (static); product NOT_MEASURED | **2** engineering |
| 2 | list JOIN pre-agg | engineering PASS (static) | 1 other class |
| 3 | additive 0047 indexes | EXPLAIN SEARCH claimed; Insights still NOT_MEASURED | 1 other class |
| 4 | GET legal no-migrate | engineering PASS (static); leave `db.batch` deferred | 1 other class |

Product measured attempts on Success bars: **0**. `NOT_MEASURED` does not spend.

Process note: 1a–4 landed in **one working tree**, against prior “do not start Wave 2 in the same diff” and “0047 only after `wave3-explain.md`”. Isolation of 1a đồng Δ is destroyed even if seed appears later (HEAD vs tree no longer a clean A/B without extra care).

---

## 4. Challenged assumption

**Team/QA:** `READY_FOR_STRATEGY_SUCCESS_PARTIAL` + shipped 1a–4 ⇒ Success-Partial (or keep executing remaining strftime).

**Counter:**

1. Success-Partial is **not** a PO packet type. Q1/vitest cannot carry a product Success claim (QA protocol: Layer A never overrides Layer B; finance needs authenticated JSON or screens).
2. Helpers exist and **are used** on `financial-by-project` / several finance GETs — but **`GET /api/dashboard/cost-summary` still implements the original P0 month-loop SCAN**. Calling the dashboard “optimized” from helper tests is a product-mapping fail (same class of error as Wave 1a vs leftover finance GETs).
3. More strftime rewrites **cannot** close Gate 0 or Insights. Those are measurement gaps. Authorizing 1c inside this arc would be grind: same lever class, no new product discriminator, attempt 3/3 without ever measuring the bar.

---

## 5. ARG

```yaml
ARG:
  product_mapping: >-
    User-visible: same đồng on finance/dashboard; faster project list / finance
    load. Engineering SQL/index changes map to that only if Layer B + rows_read
    or Insights confirm. They do not, yet. cost-summary still old SCAN loop.
  physical_metric_definition: >-
    Labor KPI = timesheets × monthly_labor_costs (TU-DIEN), not project_labor_costs
    cache. D1 cost = rows scanned, not statement count. Progress = task done/open.
  measured_owner: co-dominant
    # remaining SCAN: dashboard/cost-summary + labor-costs/sync + analytics COUNT
  lever_upper_bound: >-
    Another strftime→range rewrite on cost-summary/sync can cut those SCAN loops
    but cannot prove Δ=0 đồng or prod avgRowsRead. 0047 SEARCH proves planner
    can use work_date index; empty-table EXPLAIN does not prove billing drop.
  retained_contract_risk:
    - NTC start_day != 1 clip vs full-month semantics
    - substr(work_date,1,4)/(6,2) ISO assumption
    - GET labor-costs still cache-first (TU-DIEN KPI forbids cache) — pre-existing
    - calendarMonthsOrFilter interpolates date literals
    - 0047 not applied to production D1 in this evidence set
  aps_hard_limits_respected: n/a
  measured_attempts_spent: 2 in labor-strftime class (product 0)
  grind_verdict: CLOSE  # more SQL in this box does not close the product bar
```

---

## 6. Decision

**`STRATEGY_CLOSE_CEILING`** — not `STRATEGY_APPROVE_EXECUTION`, not `STRATEGY_PO_PACKET`.

| Rejected | Why |
|---|---|
| `STRATEGY_APPROVE_EXECUTION` | Remaining strftime is labeled out of box; further same-class SQL cannot close Layer B/Insights; grind. |
| `STRATEGY_HOLD_MEASURE` | Measurement is still valuable **later**, but this **authorized engineering arc** is already fully shipped; holding would keep an open box without a new discriminator. |
| `STRATEGY_PO_PACKET` (Success) | Precheck FAIL — decision metrics NOT_MEASURED. |
| `STRATEGY_PO_PACKET` (now) | Mission: do not Telegram; PO reviews later. Ceiling **content** is prepared; send only when PO asks. |

```yaml
ceiling_scope:
  arc: D1 query optimization (waves 0–4 engineering)
  lever_set:
    - labor helpers GROUP BY date-range (finance.ts)
    - finance GET reuse of aggregators / fiscalMonthsSQLFilter half-open
    - JOIN pre-agg GET /api/projects + /api/executive/projects
    - shared-costs IN; weekly-plan assignee batch; legal no ld.*
    - migrations/0047 work_date indexes
    - GET legal overview no auto-migrate
  architecture_assumptions:
    - half-open work_date range + index beats strftime SCAN
    - JOIN pre-agg beats correlated COUNT on project lists
  product_ceiling_claimed: false
```

This ceiling **closes the current route**, not the product objective “dashboard/finance nhanh hơn, số không đổi”. Reopen only with a **new** box: (measurement) Layer B + Insights, or (new lever) remaining SCAN paths.

---

## 7. PO_PACKET_PRECHECK (Success — must not send)

```yaml
PO_PACKET_PRECHECK:
  decisive_metrics_measured: false
  product_evidence_supports_claim: false
  run_class_valid_for_product_claim: false
  open_NOT_MEASURED_on_decision_metric: true
  technical_options_to_PO: false
  result: FAIL
```

**Ceiling packet (when PO asks)** — claim is “route closed, product not proven”; that claim **is** supported. Do not attach Option A/B/C or harness rem.

---

## 8. Draft PO packet (Vietnamese, send only when PO asks)

**Không gửi Telegram trong lượt này.**

```text
Kết quả: Đã đóng vòng tối ưu truy vấn. Phần kỹ thuật: danh sách dự án không còn đếm công việc lặp từng dòng; một phần màn hình tài chính gom giờ theo khoảng ngày; đã thêm chỉ mục ngày công. Chưa đối soát số tiền trên màn hình thật (cùng dự án, trước/sau). Chưa đo chi phí đọc dữ liệu trên hệ thống đang chạy. Vì vậy chưa xác nhận người dùng thấy tải nhanh hơn hay số lương không đổi.

PO cần: Xác nhận đóng vòng này với bằng chứng sản phẩm chưa đủ, hoặc yêu cầu vòng đo riêng (đăng nhập thử, so số lương trên các màn tài chính, xem báo cáo sử dụng cơ sở dữ liệu production). Không đổi thêm công thức cho đến khi có lệch số.

Chi tiết: Agents Logs/004.advisor-strategy-d1-query-close-2026-08-25.md
```

---

## 9. TEAM_DIRECTIVE

- **Stop code** in this arc. No Wave 1c/5, no more 0047 columns, no dashboard/sync rewrite until a **new** authorized box.
- **Do not** tell PO this is Success or “đã nhanh hơn”.
- **Do not** invent Insights/`rows_read`/Δ đồng.
- If PO later asks for the packet: send the draft above only (short VN). No logs dump.
- Follow-up (not this box): Gate 0 seed+token; `wrangler d1 insights`; apply 0047 on prod + one `PRAGMA optimize`; then optionally a new box for `dashboard/cost-summary` + `labor-costs/sync`.

```yaml
STRATEGY_FOOTER:
  strategyOutputType: STRATEGY_CLOSE_CEILING
  challenged_assumption: SUCCESS_PARTIAL from vitest + shipped 1a–4 equals product Success
  po_packet_precheck: FAIL
  next_agent: none
```
