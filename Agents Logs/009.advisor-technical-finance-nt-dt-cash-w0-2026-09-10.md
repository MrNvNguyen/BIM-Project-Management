# Technical advisor — finance NT/DT/cash Wave 0 harness

**Date:** 2026-09-10  
**Directive:** `FIN-NT-DT-CASH-W0-HARNESS` (strategy `STRATEGY_APPROVE_EXECUTION`)  
**Authority:** `finance_nt_dt_cash` box via plan attach; SSOT `docs/TU-DIEN-SO-LIEU.md` + `src/finance.ts`  
**PO-human:** NO

## PO-visible problem (Wave 0 scope)

Dashboard / executive / costs-summary still SUM **gross** NT/cash and **exclude pending** from booked (Wave 1+). Wave 0 does **not** change screens — it locks helper contracts so later waves cannot silently re-ship gross-as-NT or double-VAT via `enrichRevenueRow`.

## Owner profile

```yaml
owner_profile:
  DOMINANT_OWNER: finance
  TWO_CO_DOMINANT_OWNERS: []
  DISTRIBUTED_COST_PROFILE: n/a
```

## WAVE_CARD

```yaml
WAVE_CARD:
  directive_id: FIN-NT-DT-CASH-W0-HARNESS
  wave_id: W0-tests
  product_problem: >-
    KPI surfaces will keep wrong NT/GTTT/booked until status-aware aggregation
    and enrichRevenueRow row-shape are test-locked. enrichRevenueRow may
    re-VAT booked project_revenues.amount as gross acceptance (orphan / zero-NT).
  measured_owner: finance
  hypothesis: >-
    Adding aggregateThreeMoney (status-filtered NT/cash/pending NT; cancelled out;
    booked NOT inside aggregate) plus enrichRevenueRow fixture tests will fail on
    current fallbackAcceptance || row.amount path; a surgical enrichRevenueRow fix
    (only if red) locks DT_NS/NT/GTTT semantics before Wave 1 route wiring.
  prediction:
    metric: npm_test_finance_helpers
    expected_range: >-
      all vitest green including new describe blocks; pending fixture
      gross 1_100_000 VAT10 fee30 → amount_before_vat=1_000_000,
      booked_revenue=700_000, cash_collected=0, cash_before_vat=0;
      aggregateThreeMoney mix-status sums match hand-computed NT/cash/pending NT
      at VAT 8% and 10%; cancelled rows contribute 0; existing
      computeBookedRevenue(1_100_000,10,30) tests unchanged at 700_000 booked.
    written_before_measurement: true
  falsifier: >-
    New tests pass while encoding (a) gross SUM(amount) as NT,
    (b) cash from pending rows, (c) cancelled rows in totals,
    (d) project_revenues.amount (booked) fed into computeBookedRevenue as gross NT,
    or (e) booked folded into aggregateThreeMoney return shape.
  one_allowed_lever: >-
    src/finance.ts + src/finance.test.ts only: export aggregateThreeMoney;
    add tests; surgical enrichRevenueRow body fix only if enrichRevenueRow tests fail.
  contracts_at_risk: [finance-ssot]
  qa_tier: Q1
  attempt_number: 1/3
  stop_rule: >-
    Close Wave 0 when npm test green on new cases. Forbidden in same diff:
    index.tsx, app.js, executive-dashboard.js, migrations, TU-DIEN, DELETE authz,
    resync, GET /api/revenues COALESCE, computeBookedRevenue change, Wave 1 KPI routes.
    Do not claim product PASS or same_three_numbers_across_surfaces.
```

## DoD checklist (lead-dev)

See parent handoff message.

## Route

**APPROVE-HANDOFF** → `lead-dev`
