# Strategy advisor — finance_nt_dt_cash Wave 2+3 authorize

**Date:** 2026-09-10  
**Product:** BIM Project Management (Hono + D1 + SPA) — **not** OneX/Forma/Electron  
**Authority:** PO-attached plan `finance_nt_dt_cash_15604ff2.plan.md` Waves 2–3. SSOT: `docs/TU-DIEN-SO-LIEU.md` + `src/finance.ts`.  
**PO-human:** **NO** — no Success / Ceiling / Visual. Do not send `STRATEGY_PO_PACKET`. Do not Telegram.

```yaml
strategyOutputType: STRATEGY_APPROVE_EXECUTION
authorized_box: finance_nt_dt_cash
wave_1_product: NOT_MEASURED
wave_2_3:
  directive_id: FIN-NT-DT-CASH-W2W3-REMAINING
  one_allowed_lever: >-
    Remaining KPI/authz callers: analytics year+lifetime + analytics/financial
    booked includes pending; contract_progress NT or GTTT vs GTHĐ;
    budget_progress = doanh_thu_ns/NS; resync amount>0; comments 13038/0037;
    GET /api/revenues no COALESCE-to-booked; PUT vat clamp;
    DELETE isProjectAdminOrAbove; app.js list prefer API fields
challenged_assumption: >-
  W1 Q2 NOT_MEASURED requires HOLD before any more money/authz routes,
  or 27/27 means W1 product is closed.
po_packet_precheck: N/A
next_agent: technical-advisor
```

---

## Wave 1 adjudication (engineering close; not product)

| id | statement | evidence | status |
|---|---|---|---|
| W1-C1 | Six P0 routes call `aggregateThreeMoney` (or overwrite list fields from it). Booked = `SUM(project_revenues.amount)` paid+partial+pending. Period pending booked uses `request_date` join. | `src/index.tsx` L5277–5295, L5446–5467, L7927–8044, L15932–15949, L16107–16131, L16385–16427 | **measured** (engineering) |
| W1-C2 | `computeBookedRevenue` body unchanged; fixture 1.1tr / VAT10 / fee30 still 700_000. `src/finance.test.ts` has **27** `it(` cases. | `src/finance.ts` L9–19; `src/finance.test.ts` 27 tests | **measured** (engineering) |
| W1-C3 | Executive overview: GTTT/debt/collected_pct from `cashBeforeVat`; `debt = max(0, GTHĐ − cashBeforeVat)`; `recent_payments` maps `enrichPaymentMetrics`; `total_revenue` lifetime pending-in. | L16390–16486 | **measured** (engineering) |
| W1-C4 | TU-DIEN: DT vào sổ gồm pending đã NT; công nợ HĐ = GTHĐ − GTTT trước VAT. | `docs/TU-DIEN-SO-LIEU.md` L21 | **measured** (doc) |
| W1-C5 | costs-* `pending_revenue` is **all-time** pending NT (no `request_date` period). Booked path is period-correct. | L5291–5295, L5463–5467 | **valid leftover** vs W1 period-scope ask |
| W1-C6 | Layer B / auth API on the six W1 routes; same-three-numbers including analytics | no `docs/evidence/` for this box; no live probe this turn | **NOT_MEASURED** |

**Invalid environment:** none.  
**Valid product pass (box bar `same_three_numbers_across_surfaces`):** **none**.  
**W1 product close:** **NO**. Do not spend W1 attempt cap (`NOT_MEASURED`). Do not revert the uncommitted W1 tree.

---

## Independent claims (W2/W3 need — code, not team narrative)

| id | statement | evidence | class |
|---|---|---|---|
| C1 | `GET /api/analytics/financial-by-project`: `revenue_collected` / `revenue_total` = paid+partial **and** `revenue_date` (pending dropped). `revenue_pending` = **gross** `SUM(payment_requests.amount)` **all-time**. Profit/margin use `revenue_collected` without pending. | L11229–11331 | **valid product failure** |
| C2 | `contract_progress` = `revenue_total` (booked, paid+partial) / GTHĐ; `budget_progress` = `revenue_total` / NS. Booked already net of fee → % vs GTHĐ is mixed-unit. Plan: % NT or % GTTT vs GTHĐ; % DT = `doanh_thu_ns` / NS. | L11332–11335, L11404–11407 | **valid product failure** |
| C3 | Lifetime route: `revenue_collected` paid+partial only; `revenue_pending` gross all-time; same mixed-unit progress. `revenue_total` = `SUM(amount)` all statuses (includes pending booked but also any cancelled revenue row). | L11445–11541 | **valid product failure** |
| C4 | `GET /api/analytics/financial` monthly + top-projects: paid+partial + `revenue_date` only. Pending NT never enters the month series. | L10951–10958, L11080–11085 | **valid product failure** (same class; include) |
| C5 | `GET /api/dashboard/cost-summary`: `total_revenue` paid+partial + `revenue_date`; `pending_revenue` = **gross** all-time pending. | L8299–8303 | **valid product failure** (same class; include) |
| C6 | Resync project + all: `status IN ('paid','partial') AND amount > 0` — pending with `amount > 0` never re-synced after fee change. `amount > 0` already present. | L13458–13459, L13487–13488 | **valid product failure** |
| C7 | Stale comments: module E L13038 “sync khi paid \| partial”; migration 0037 L10–13 “DT = **paid_amount** / (1+vat)”. SSOT uses **`amount`** (NT) for booked. | L13038; `migrations/0037_add_vat_to_payments.sql` L10–13 | **measured** (doc drift) |
| C8 | `GET /api/revenues` paid query: `paid_amount_original = COALESCE(pq.amount, pr.amount)` — orphan / `pq.amount=0` feeds booked as gross into `enrichRevenueRow`. Pending branch already uses `pq.amount`. | L4528–4531 | **valid product failure** |
| C9 | `enrichRevenueRow` already refuses booked-as-gross (W0). W3 helper work is **done**; COALESCE still poisons the paid-query input. | `src/finance.ts` L168–181 | **valid product pass** (helper) / **fail** (caller) |
| C10 | POST payments clamps `vat_pct` 0–100. PUT writes `vat_pct` raw. | L13257 vs L13329–13335 | **valid product failure** |
| C11 | `DELETE /api/legal/payments/:id`: `authMiddleware` only — no `isProjectAdminOrAbove`. POST/PUT already gate. Deletes payment + linked revenue. | L13429–13446 vs L13249, L13323 | **measured** authz gap |
| C12 | `app.js` revenues list (~9476) prefers `amount_before_vat` / `cash_before_vat` / `booked_revenue` but still fallbacks `calcRevenueNet`. HSPL list (~19754) same pattern. | `public/static/app.js` L9476–9482 | **valid leftover** (W3) |
| C13 | `estimate-vs-actual.doanh_thu_ns` already `IN ('paid','partial','pending')`; NT/GTTT via `amountExcludingVat`. | L13128–13148 | **valid product pass** — **do not touch** |

**Invalid environment:** none.  
**Valid product failure (this directive):** C1–C8, C10–C12.  
**Valid product pass:** C9 helper, C13.  
**NOT_MEASURED:** Layer B same-three-numbers; W1 six-route live API; production D1 seed of pending 1.1tr.

---

## Challenged assumption

**Team / parent fork:** “HOLD until W1 Q2, because Wave 1 stop_rule said strategy re-enters after Q2 before Wave 2 — **or** 27/27 + code review closes W1 so W2+W3 are polish.”

**Counter:**

1. `NOT_MEASURED` does not spend the W1 cap and does not make analytics/revenues/DELETE unknown. Those failures are independently visible in source (C1–C11). HOLD would freeze remaining P1/P2 screens for a measurement that cannot change whether `revenue_collected` still drops pending.
2. 27/27 is the **W0 harness**, not W1 product. W1 engineering is coherent enough to **copy the same helper contract** to remaining callers — it is **not** product PASS and **not** box SUCCESS.
3. Combining W2+W3 is one remaining-caller class (plan already scoped both). It is **not** a new formula and **not** Wave 4 Layer B. Do not reopen `estimate-vs-actual` or `computeBookedRevenue`.

---

## ARG

```yaml
ARG:
  product_mapping: >-
    User-visible DT NS / % vs GTHĐ / % vs NS on analytics tables;
    revenues list NT on orphan rows; DELETE payment by any authed user.
  physical_metric_definition: >-
    Same W0/W1 contract. NT = amount/(1+vat) pending+partial+paid;
    GTTT = paid_amount/(1+vat) paid+partial; DT_NS = project_revenues.amount
    paid+partial+pending (period pending by request_date).
    contract_progress = NT or GTTT vs GTHĐ (not booked/GTHĐ).
    budget_progress = doanh_thu_ns / computeProjectBudget.
  measured_owner: finance
  lever_upper_bound: >-
    Can close remaining P1/P2 callers in the signed plan.
    Cannot close Layer B all-surfaces, W1 live PASS, or box SUCCESS.
  retained_contract_risk:
    - computeBookedRevenue must not change
    - do not fold booked into aggregateThreeMoney
    - booked = SUM project_revenues.amount, never re-VAT
    - no VAT/fee math only in app.js (list prefer API; keep calcRevenueNet for form preview only)
    - estimate-vs-actual doanh_thu_ns do not rewrite
    - D1: no per-project N+1; no SELECT * on list paths (resync may keep SELECT * of filtered payments)
    - DELETE must reuse isProjectAdminOrAbove (same helper as PUT)
  aps_hard_limits_respected: n/a
  measured_attempts_spent: 0
  grind_verdict: not_grinding
```

---

## PO_PACKET_PRECHECK

Not a PO packet. If attempted: **FAIL** (`decisive_metrics_measured: false`, W1/W2 product `NOT_MEASURED`).

---

## TEAM_DIRECTIVE (Wave 2+3 as one remaining-surfaces close)

```yaml
TEAM_DIRECTIVE:
  directive_id: FIN-NT-DT-CASH-W2W3-REMAINING
  authorized_box: finance_nt_dt_cash
  wave_id: W2W3-remaining
  attempt: 1/3
  qa_tier: Q2
  measured_owner: finance
  hypothesis: >-
    Analytics / cost-summary / resync still treat DT NS as paid+partial
    (and/or revenue_date), so pending NT never enters booked or % NS.
    Revenues COALESCE still feeds booked as gross NT. PUT vat is unclamped.
    DELETE payment has no project-admin gate.
  prediction:
    metric: w2w3_remaining_surfaces
    expected_range: >-
      Year analytics: in-period pending 1.1tr / VAT10 / fee30 →
      revenue_collected (DT NS) 700_000, revenue_collected_original (NT) 1_000_000,
      paid_amount_total (GTTT) 0; contract_progress = NT or GTTT vs GTHĐ
      (not booked/GTHĐ); budget_progress = 700_000 / NS.
      Lifetime: same units, pending included without revenue_date.
      Resync pending amount>0 updates project_revenues.
      GET /api/revenues orphan: amount_before_vat not a second VAT strip of booked.
      PUT vat_pct 150 → stored 100 (or 400). DELETE non-admin → 403; row remains.
    written_before_measurement: true
  falsifier: >-
    revenue_collected still paid+partial only; year pending still all-time gross
    dumped into every FY; contract_progress still booked/GTHĐ; resync skips pending;
    COALESCE still present; DELETE still 200 for member; computeBookedRevenue
    or estimate-vs-actual changed; or Q1-only used to claim product PASS.
  one_allowed_lever: >-
    src/index.tsx remaining callers + comment/0037 note + surgical app.js list
    prefer-API only:
    (1) GET /api/analytics/financial-by-project and -lifetime:
        revenue_collected = SUM project_revenues.amount paid+partial+pending
        (year: pending period = request_date join; paid/partial = revenue_date;
         lifetime: status IN, no date trap). revenue_pending = pending NT
        before VAT (cash-sense), not subtracted from DT. profit/margin on
        revenue_collected including pending.
        contract_progress = NT or GTTT vs GTHĐ (expose which; do not use booked/GTHĐ).
        budget_progress = doanh_thu_ns / project_budget (computeProjectBudget).
    (2) GET /api/analytics/financial monthly + top-projects: same booked-includes-pending
        period rule (pending month/year by request_date).
    (3) GET /api/dashboard/cost-summary: total_revenue booked including pending
        (period pending by request_date); pending_revenue = pending NT before VAT.
    (4) Allowed one-liner: costs-revenue-summary / costs-summary pending_revenue
        period-scope by request_date (W1 leftover). Do not reopen booked math.
    (5) resync-revenues + resync-revenues-all: every payment amount>0
        (drop paid/partial-only status cut). Keep amount>0.
    (6) Comments only: index.tsx ~13038 sync-on-amount>0; migrations/0037
        booked from amount (NT) not paid_amount.
    (7) GET /api/revenues paid query: paid_amount_original = pq.amount
        (NULL if no payment / zero — do not COALESCE to pr.amount).
        Do not change enrichRevenueRow unless a new test fails.
    (8) PUT /api/legal/payments/:id: clamp vat_pct 0–100 like POST before write/sync.
    (9) DELETE /api/legal/payments/:id: isProjectAdminOrAbove on payment.project_id;
        403 otherwise; do not delete.
    (10) public/static/app.js revenues list (~9476): prefer API
        amount_before_vat / cash_before_vat / booked_revenue; drop calcRevenueNet
        on that list if those fields are always present. Keep calcRevenueNet
        for HSPL form preview only. Do not recompute analytics % in JS.
  contracts_at_risk: [finance-ssot, auth-rbac, d1-query]
  stop_rule: >-
    Stop this wave when the listed routes match the prediction, verified Q2
    (curl/API against running app; fingerprint the working-tree SHA, not parent
    d8720e0 alone). W1 six routes may ride as regression on the same probe —
    still label W1 product NOT_MEASURED if that probe is skipped.
    Forbidden in the same diff: computeBookedRevenue body, estimate-vs-actual
    doanh_thu_ns rewrite, migrations schema (comment-only on 0037 OK),
    Wave 4 Layer B SUCCESS claim, box SUCCESS, app.v2.js.
    Do not claim same_three_numbers Layer B or box SUCCESS.
  must_not_claim_pass_yet:
    - W1 product PASS (still NOT_MEASURED until live auth probe)
    - same_three_numbers Layer B / Wave 4
    - box SUCCESS
  next_agent: technical-advisor
```

Technical writes **one** `WAVE_CARD` for `FIN-NT-DT-CASH-W2W3-REMAINING`, then lead-dev **one** lever, then qa-agent **Q2**. Strategy re-enters after Q2 report-up before Wave 4 / box close. No PO-human.
