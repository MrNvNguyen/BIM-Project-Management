# Strategy advisor — finance_nt_dt_cash Wave 0 authorize

**Date:** 2026-09-10  
**Product:** BIM Project Management (Hono + D1 + SPA) — **not** OneX/Forma/Electron  
**Authority:** PO-attached plan `finance_nt_dt_cash_15604ff2.plan.md` + `/multi-agents triển khai`. No `0 Documents/1-Sprints/` / CAMPAIGN-STATE. SSOT số liệu: `docs/TU-DIEN-SO-LIEU.md` + `src/finance.ts`.  
**PO-human:** **NO** — box already authorized via plan attach. Do not send `STRATEGY_PO_PACKET`. Do not Telegram.

```yaml
strategyOutputType: STRATEGY_APPROVE_EXECUTION
authorized_box: finance_nt_dt_cash
wave_0:
  directive_id: FIN-NT-DT-CASH-W0-HARNESS
  one_allowed_lever: finance.ts helper contract lock (aggregateThreeMoney + enrichRevenueRow tests; surgical enrichRevenueRow only if tests fail)
challenged_assumption: >-
  Prior chat's HSPL/analytics cash_before_vat / amountExcludingVat work is not
  product PASS for the three-number bar. Dashboard, executive, and costs-summary
  still SUM gross and exclude pending from booked.
po_packet_precheck: N/A
next_agent: technical-advisor
```

---

## Independent claims (code, not team narrative)

| id | statement | evidence | status |
|---|---|---|---|
| C1 | `computeBookedRevenue(1_100_000, 10, 30)` is already locked: NT 1_000_000, booked 700_000. Formula must not change. | `src/finance.test.ts` L32–36; `src/finance.ts` L9–19 | **measured** |
| C2 | Dashboard YTD still uses gross NT/cash and booked **without pending**. `acceptance_ytd` = `SUM(amount)` all statuses; `cash_ytd` = `SUM(paid_amount)` gross paid/partial; `booked_ytd` = revenues `paid`/`partial` + `revenue_date` (pending `revenue_date` is null → dropped). | `src/index.tsx` L7914–7919 | **valid product failure** vs box |
| C3 | Executive dashboard: cash/acceptance **gross**; booked **paid+partial only**. | `src/index.tsx` L15899–15913 | **valid product failure** |
| C4 | costs-revenue-summary: `revenue.value` = paid+partial only; pending is **gross** `SUM(payment_requests.amount)` standing apart; profit uses paid+partial. Comment still says pending is not “doanh thu thực tế”. | `src/index.tsx` L5276–5317, L5357–5358 | **valid product failure** |
| C5 | `aggregateThreeMoney` **does not exist**. `enrichRevenueRow` still fallbacks to `row.amount` (already booked) then re-runs `computeBookedRevenue`. No tests for either. | `src/finance.ts` L119–142; `src/finance.test.ts` (no `enrichRevenueRow` / `aggregateThreeMoney` imports) | **measured** helper gap |
| C6 | `GET /api/revenues` feeds `paid_amount_original = COALESCE(pq.amount, pr.amount)` — orphan / `pq.amount=0` looks like booked-as-gross. | `src/index.tsx` L4528–4531 | **valid product failure** (Wave 3, not W0) |
| C7 | `DELETE /api/legal/payments/:id` has `authMiddleware` only — no `isProjectAdminOrAbove`; deletes payment + linked revenue. | `src/index.tsx` L13395–13416 | **measured** authz gap (Wave 3) |
| C8 | TU-DIEN does **not** yet say DT vào sổ includes pending-with-NT. Layer B for this box: **no** `docs/evidence/` file. | `docs/TU-DIEN-SO-LIEU.md` L7–21; glob evidence = d1-query + mobile-pwa only | **NOT_MEASURED** (product claim) / **measured** (doc gap) |
| C9 | Partial prior work exists: `amountExcludingVat`, `aggregatePaymentsBeforeVat`, `cash_before_vat` on `enrichPaymentMetrics`; some `app.js` HSPL/revenues columns read `cash_before_vat`. That is **not** the three KPI surfaces in the bar. | `src/finance.ts` L22–41, L97–116; `public/static/app.js` L9479–9480, L19755, L19864 | **measured** (engineering only) |

**Invalid environment:** none (authorize from repo; no Layer B run).  
**Valid product failure:** C2, C3, C4, C6 (KPI/revenues still violate envelope).  
**Valid product pass:** **none** for `same_three_numbers_across_surfaces` / `pending_1_1m_vat10_fee30`.  
**NOT_MEASURED:** Layer B HSPL vs analytics vs executive vs dashboard vs costs-summary on one pending 1.1tr phiếu; production D1 seed of that fixture.

---

## Challenged assumption

**Team assumption:** “We already stripped VAT on HSPL/analytics and added `cash_before_vat`, so the remaining work is polish / Wave 1 can start now / Wave 0 tests are optional because `computeBookedRevenue` already has the 1.1tr fixture.”

**Counter:**

1. The box bar is **three numbers on four+ surfaces**, including dashboard, executive, and costs-summary. Those routes still SUM **gross** and **exclude pending** from booked (C2–C4). HSPL column swap ≠ those payloads.
2. `computeBookedRevenue` tests do **not** lock `enrichRevenueRow` double-VAT or status-aware `aggregateThreeMoney` (cancelled out; cash paid+partial only). Shipping Wave 1 SQL without that harness is how gross `SUM(amount)` survived the first partial fix.
3. Vitest green on helpers **cannot** close a money claim (`stop_rules`). Wave 0 close = harness exists + `npm test` green — **not** product PASS.

---

## ARG

```yaml
ARG:
  product_mapping: "Wave 0 does not change what the user sees. It locks the discriminator later waves must match."
  physical_metric_definition: "NT = amount/(1+vat) pending+partial+paid; DT_NS = booked including pending NT; GTTT = paid_amount/(1+vat) paid+partial only. Dictionary + plan. Booked stays computeBookedRevenue / project_revenues — not a new formula."
  measured_owner: finance
  lever_upper_bound: "Tests + aggregateThreeMoney + optional enrichRevenueRow surgical fix cannot close same_three_numbers_across_surfaces. They can close 'no harness / silent double-VAT in helper'. Wave 1+ owns KPI routes."
  retained_contract_risk:
    - computeBookedRevenue must not change (1.1tr/10%/30% = 700_000 locked)
    - do not fold booked into aggregateThreeMoney (booked unit ≠ NT; source is project_revenues)
    - do not compute KPI money only in app.js
    - GET /api/revenues COALESCE still poisons helper input until Wave 3
  aps_hard_limits_respected: n/a
  measured_attempts_spent: 0
  grind_verdict: not_grinding
```

---

## PO_PACKET_PRECHECK

Not run as a PO packet. If attempted: **FAIL** (`decisive_metrics_measured: false`, `product_evidence_supports_claim: false`, Layer B `NOT_MEASURED`).

---

## TEAM_DIRECTIVE (Wave 0 only)

```yaml
TEAM_DIRECTIVE:
  directive_id: FIN-NT-DT-CASH-W0-HARNESS
  authorized_box: finance_nt_dt_cash
  wave_id: W0-tests
  attempt: 1/3
  qa_tier: Q1
  measured_owner: finance
  hypothesis: >-
    Current KPI routes will keep shipping gross NT/cash and pending-excluded booked
    unless a status-aware three-money helper is specified and tested first.
    enrichRevenueRow currently re-VATs booked amount on orphan/zero-NT rows.
  prediction:
    metric: npm_test_finance_helpers
    expected_range: "green; fixture pending 1_100_000 VAT10 fee30 → NT=1_000_000, booked=700_000, cash=0"
    written_before_measurement: true
  falsifier: >-
    Tests pass while encoding gross SUM(amount) as NT, excluding pending from booked,
    or treating project_revenues.amount as gross acceptance.
  one_allowed_lever: >-
    In src/finance.ts + src/finance.test.ts only: add aggregateThreeMoney
    (acceptanceBeforeVat = pending+partial+paid; cashBeforeVat/cashGross = partial+paid;
    pendingAcceptanceBeforeVat; cancelled excluded). Add tests for that helper (VAT 8% and 10%,
    mix pending/paid, cancelled out) and for enrichRevenueRow (pending / paid / orphan /
    amount=0 — do not fallback booked amount into computeBookedRevenue as gross NT).
    Compose the 1.1tr fixture with computeBookedRevenue for DT_NS; do not put booked inside
    aggregateThreeMoney. If enrichRevenueRow tests fail against current code, surgically
    fix enrichRevenueRow only until npm test is green.
  contracts_at_risk: [finance-ssot]
  stop_rule: >-
    Close Wave 0 when npm test is green on the new cases. Do not open index.tsx, app.js,
    TU-DIEN, DELETE authz, resync, or GET /api/revenues COALESCE. Do not change
    computeBookedRevenue. Do not start Wave 1 in the same diff. Do not claim product PASS.
  must_not_claim_pass_yet:
    - same_three_numbers_across_surfaces
    - pending_1_1m_vat10_fee30 on dashboard / executive / costs-summary / analytics
    - no_gross_as_NT_vs_GTHD on those KPI routes
    - Layer B / Q2 product PASS
    - box SUCCESS
  next_agent: technical-advisor
```

Technical writes `WAVE_CARD` for this directive only, then lead-dev one lever, then qa-agent Q1 (`npm test`). Strategy re-enters after Q1 report-up **before** Wave 1 KPI routes.
