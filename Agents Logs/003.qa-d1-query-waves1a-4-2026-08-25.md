# QA — D1 query Waves 1a–4 (static + vitest)

**Date:** 2026-08-25  
**verify_sha:** `8aebf3a4ef64cd089896bc46d8b73ea1e047f7bf`  
**qa_tier:** Q1 (Layer A only; Layer B not run)  
**authority:** Engineering static + unit harness; Gate 0 blocks product đồng

## Harness

```
node node_modules/vitest/vitest.mjs run
→ 1 file, 20 passed (20), exit 0
```

## Claim matrix

| Claim | Status | Evidence |
|-------|--------|----------|
| Wave 1a: O(1) labor helpers in `finance.ts` | MEASURED | `allocateProjectLaborForCalendarMonths`, `fetchProjectHoursByMonth`, `monthDateRange`; 20/20 unit tests |
| Gate 0 labor đồng parity | NOT_MEASURED | `docs/evidence/d1-query/wave1a-parity.md` — no auth seed |
| Wave 1b: CRS / labor-costs-all / finance project use helpers not strftime loops | MEASURED | Static grep + route read: `allocateProjectLaborForCalendarMonths` or `fetchAllProjectsHoursByMonth` + aggregates |
| Wave 1b: `fiscalMonthsSQLFilter` half-open | MEASURED | Delegates to `calendarMonthsOrFilter` → `monthDateRange` |
| Wave 1b: GET labor-costs uncached no strftime | MEASURED | `fetchProjectHoursByMonth` + `monthDateRange` |
| Wave 2: GET `/api/projects` + `/api/executive/projects` JOIN pre-agg | MEASURED | No `(SELECT COUNT(*) FROM tasks` in those routes; LEFT JOIN `ts` subquery |
| Wave 2: shared-costs one IN query | MEASURED | GET `/api/shared-costs` batch allocations |
| Wave 2: weekly-plan batch assignees | MEASURED | Single IN query for all assignee ids |
| Wave 2: legal overview no `ld.*` | MEASURED | Explicit column list; no `SELECT ld.*` |
| Wave 3: migration 0047 | MEASURED | `migrations/0047_timesheets_work_date_indexes.sql` |
| Wave 4: legal overview no migrate on GET | MEASURED | Comment + no call; migrate only POST |

## Deferred / out of scope

- `POST /api/projects/:id/labor-costs/sync` still uses `strftime` on timesheets (Wave 1b defer).
- `GET /api/monthly-labor-costs` enrich loop still strftime per row.
- Dashboard analytics loop ~8035 still strftime (not in wave verify list).
- `/api/analytics/financial-by-project` still correlated task COUNT (not claimed for Wave 2 routes).

## Layer B

Not run — no local Worker+D1 dev session, no admin Bearer token. Product PASS on đồng/screens forbidden.

## Verdict

**READY_FOR_STRATEGY_SUCCESS_PARTIAL** — Layer A claims measured; Gate 0 + Layer B open.
